/**
 * Imports styled
 */
import { styled } from '@mui/system';

/**
 * Styles the Title
 */
export const Container = styled('div')(() => {
  return {
    textAlign: 'center',
    display: 'inline-block',
    width: 250,
    marginTop: 0,
  };
});
