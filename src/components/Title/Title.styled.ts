/**
 * Imports styled
 */
import { styled } from '@mui/system';

/**
 * Styles the Container
 */
export const Container = styled('h1')(() => {
  return {
    fontSize: '4rem',
    marginBottom: '0.1em',
    marginTop: 0,
  };
});
