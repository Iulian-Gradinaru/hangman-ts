/**
 * Imports styled
 */
import { styled } from '@mui/system';

/**
 * Styles the Title
 */
export const Container = styled('h1')(() => {
  return {
    fontSize: '4rem',
    fontWeight: 100,
    marginBottom: '0.1em',
    marginTop: 0,
  };
});
