/**
 * Imports styled
 */
import { styled } from '@mui/system';

/**
 * Styles the Word
 */
export const Container = styled('div')(() => {
  return {
    letterSpacing: '1em',
    margin: '0.4em -1em 0.2em 0',
    fontSize: '2rem',
  };
});
